// secret key to generate token
const config = {
    secretKey : 'lfjeoijf9hj32fknfn3okmawn'     // post-submission edit: anyone seeing this on github, doesn't matter, I am not hosting this anyway
};

module.exports = config;